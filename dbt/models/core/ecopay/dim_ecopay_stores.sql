{{ 
    config(
        materialized='materialized_deduplicate',
        unique_key='id',
        timestamp_column='ts_ms',
        priority_sort_columns=['ts_ms'],
        tags=["ecopay", "core", "dim", "store"]
    ) 
}}

SELECT
    id,
    __v,
    approved_at,
    bank_account_name,
    bank_account_number,
    bank_code,
    bank_integrated,
    bank_name,
    branch_name,
    business_name,
    category_code,
    code,
    contract_codes,
    created_at,
    created_by,
    crm_org_id,
    eco_wallet_holder,
    ecom_config,
    ewallet_account,
    expires_payment_link_time,
    histories,
    index,
    integrated_partner,
    ipn_url,
    is_allowed_create_payment_link,
    is_default,
    is_supplier,
    location,
    location_v2,
    manager_phone,
    manager_team_id,
    mapping_at,
    merchant_code,
    my_qrcode_config,
    name,
    note,
    notification_channel_config,
    notification_config,
    payment_link_config,
    payment_methods,
    payment_type,
    phone,
    pos_config,
    pure_name,
    qrcode,
    qrcode_list,
    qrcode_mapping_code,
    qrcode_mapping_code_list,
    reasons,
    reconcile_method,
    refund_ipn_url,
    rejected_at,
    rejected_count,
    representative_user_info,
    request_create_from,
    sale_info,
    secret_key,
    softPOS_config,
    status,
    steps,
    tax_code,
    terminal_name,
    transaction_config,
    updated_at,
    updated_by,
    updatedAt,
    user_fee_configs,
    zalo,
    ts_ms,
    inserted_at as source_inserted_at,
    CURRENT_TIMESTAMP() AS inserted_at
FROM {{ ref('stg_ecopay_stores') }}
